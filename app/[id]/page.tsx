import { Modal } from "@/components/ui/Modal";

export default function PreviewPage({ params }: { params: { id: string } }) {
  return (
    <Modal>
      {/* {params.id} */}
      {/* <Login />/ */}
    </Modal>
  );
}
